apiVersion: v1
kind: Service
metadata:
  name: '{{ include "name" . }}-postgresql'
spec:
  type: ExternalName
  externalName: '{{ include "name" . }}-cloudnative-pg-cluster-pooler-rw.{{ .Release.Namespace }}.svc.cluster.local'
  ports:
    - name: pgbouncer
      port: 5432
      protocol: TCP
      targetPort: pgbouncer
