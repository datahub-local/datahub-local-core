---
apiVersion: v1
kind: Secret
metadata:
  name: "{{ template "superset.secret" . }}-env"
  namespace: "{{ .Release.Namespace }}"
type: Opaque
stringData:
  REDIS_HOST: "{{ .Values.release_name }}-valkey-headless"
  REDIS_USER: "default"
  REDIS_PORT: "6379"
  REDIS_PROTO: "redis"
  REDIS_DB: "1"
  REDIS_CELERY_DB: "0"
  DB_HOST: "{{ .Values.release_name }}-postgresql-hl"
  DB_PORT: "5432"
  DB_USER: "{{ .Values.security_admin_user }}"
  DB_NAME: "postgres"