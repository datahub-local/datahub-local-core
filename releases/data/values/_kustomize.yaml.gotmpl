#{{- $namespace := "data" }}

kustomize:
  ollama:
    - apiVersion: apps/v1
      kind: Deployment
      metadata:
        name: ollama
      spec:
        template:
          spec:
            dnsPolicy: "None"
            dnsConfig:
              nameservers:
                - 8.8.8.8
